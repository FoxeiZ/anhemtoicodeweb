@model IEnumerable<anhemtoicodeweb.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = ViewBag.Layout;
}

@*<h2>Danh sách các hãng</h2>*@
<h4>Sửa bảng <b>hãng sản phẩm</b></h4>

<p>
    @Html.ActionLink("Tạo mới", "Create", "", new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NameCate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameCate)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.IDCate.Trim() }) |
                @Html.ActionLink("Chi tiết", "Details", new { id = item.IDCate.Trim() }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.IDCate.Trim() })
            </td>
        </tr>
    }

</table>
